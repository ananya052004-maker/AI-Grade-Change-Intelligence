server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: any path falls back to index.html (no client-side router in
    # this build, but this keeps a direct deep-link refresh from 404ing).
    location / {
        try_files $uri /index.html;
    }
}
